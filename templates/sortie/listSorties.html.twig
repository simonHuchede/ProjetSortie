{% extends 'base.html.twig' %}

{% block contenu %}
    <div class="text-center list-unstyled">

    {% for sortie in listSorties %}
            <li><a href="{{ path('sortie_sortieDetail', {'sortie': sortie.id}) }}">{{ sortie.nom }}</a></li>
    {% endfor %}

    </div>
{% endblock %}

